name: Post Merge E2E

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  repository-projects: write

jobs:
  post_merge_e2e:
    runs-on: [self-hosted, k3s-local]
    env:
      GH_TOKEN: ${{ github.token }}
      APP_NAMESPACE: ljwx-platform
      APP_SERVICE: ljwx-platform
      APP_SERVICE_PORT: "80"
      MONITORING_NAMESPACE: monitoring
      PROM_SERVICE: prometheus-kube-prometheus-prometheus
      LOKI_NAMESPACE: logging
      LOKI_SERVICE: loki
      TRACING_NAMESPACE: tracing
      TEMPO_SERVICE: tempo
      SERVICE_NAME: ljwx-platform
      STRICT_TEMPO_SEARCH: "0"
      TENANT_A_USER: ${{ secrets.E2E_TENANT_A_USER || 'admin' }}
      TENANT_A_PASS: ${{ secrets.E2E_TENANT_A_PASS || 'Admin@12345' }}
      TENANT_B_USER: ${{ secrets.E2E_TENANT_B_USER || 'tenantB_admin' }}
      TENANT_B_PASS: ${{ secrets.E2E_TENANT_B_PASS || 'Admin@12345' }}
      K6_VUS_R10: "1"
      K6_ITERATIONS_R10: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install kubectl (no sudo)
        shell: bash
        run: |
          set -euo pipefail
          BIN_DIR="${RUNNER_TEMP}/bin"
          mkdir -p "${BIN_DIR}"
          KUBECTL_BIN="kubectl"
          if ! command -v kubectl >/dev/null 2>&1; then
            curl -sSL -o "${BIN_DIR}/kubectl" "https://dl.k8s.io/release/$(curl -sSL https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x "${BIN_DIR}/kubectl"
            echo "${BIN_DIR}" >> "${GITHUB_PATH}"
            KUBECTL_BIN="${BIN_DIR}/kubectl"
          fi
          "${KUBECTL_BIN}" version --client

      - name: Validate tools
        shell: bash
        run: |
          set -euo pipefail
          command -v jq >/dev/null
          command -v kubectl >/dev/null
          command -v curl >/dev/null

      - name: Configure kubeconfig
        shell: bash
        run: |
          set -euo pipefail
          if [ -n "${{ secrets.KUBE_CONFIG_B64 }}" ]; then
            KUBECONFIG_PATH="${RUNNER_TEMP}/kubeconfig"
            echo "${{ secrets.KUBE_CONFIG_B64 }}" | base64 -d > "${KUBECONFIG_PATH}"
            chmod 600 "${KUBECONFIG_PATH}"
            echo "KUBECONFIG=${KUBECONFIG_PATH}" >> "${GITHUB_ENV}"
            exit 0
          fi
          if [ -r "/etc/rancher/k3s/k3s.yaml" ]; then
            echo "KUBECONFIG=/etc/rancher/k3s/k3s.yaml" >> "${GITHUB_ENV}"
            exit 0
          fi
          echo "[FATAL] kubeconfig not found. Provide KUBE_CONFIG_B64 or make /etc/rancher/k3s/k3s.yaml readable."
          exit 1

      - name: Verify cluster access
        shell: bash
        run: |
          set -euo pipefail
          kubectl cluster-info
          kubectl get nodes -o wide

      - name: Run R10 E2E smoke on k3s
        id: r10
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          TMP_DIR=/tmp/ljwx-gate-results
          mkdir -p "$TMP_DIR"
          rm -f "$TMP_DIR"/R*.json

          status="PASS"
          if ! RUN_R10=1 RUN_R11=0 bash scripts/check-observability-k3s.sh; then
            status="FAIL"
          fi

          REPORT_FILE="/tmp/ljwx-gate-results/observability-e2e.json"
          r10_status="$(jq -r '.gates.r10.status // "UNKNOWN"' "${REPORT_FILE}" 2>/dev/null || true)"
          if [[ -z "${r10_status}" || "${r10_status}" == "null" ]]; then
            r10_status="$status"
          fi

          bash scripts/gen-rtm.sh || true
          bash scripts/gates/gate-report.sh 00 "$TMP_DIR"
          bash scripts/gates/gate-summary.sh

          echo "r10_status=${r10_status}" >> "$GITHUB_OUTPUT"

      - name: Create regression bug issue on R10 failure
        if: always() && steps.r10.outputs.r10_status == 'FAIL'
        shell: bash
        run: |
          set -euo pipefail
          run_url="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          gh issue create \
            --repo "${GITHUB_REPOSITORY}" \
            --title "[Bug][Regression][P0] Cross-tenant access must return 404 (post-merge e2e)" \
            --label "type:bugfix" \
            --label "priority:P0" \
            --label "gate:fail" \
            --body "Automated regression detection from post-merge E2E.\n\n- Run URL: ${run_url}\n- Gate: R10\n- Expected: cross-tenant read/update/delete MUST return HTTP 404\n- Actual: one or more checks failed in E2E smoke\n\nPlease triage and link fix PR." || true

      - name: Update weekly regression campaign
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          status="${{ steps.r10.outputs.r10_status }}"
          if [[ -z "$status" ]]; then status="FAIL"; fi
          if ! bash scripts/regression/regression-weekly.sh --mode post-merge --status "$status" --apply; then
            echo "weekly campaign update failed; continue workflow" >&2
          fi

      - name: Upload gate reports for Pages
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gate-reports
          path: |
            docs/reports/**
            tests/results/**
            /tmp/ljwx-gate-results/**
            /tmp/ljwx-gate-results/observability/**

      - name: Fail workflow when R10 fails
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          status="${{ steps.r10.outputs.r10_status }}"
          if [[ "$status" != "PASS" ]]; then
            echo "R10 failed: ${status:-unknown}" >&2
            exit 1
          fi
