# LJWX Platform 规格文档

本文件为入口指引，不包含实际规格内容。

- **AI Agent**: 请读取 `CLAUDE.md`（自动加载），然后按工作流程读取 `spec/phase/phase-{NN}.md`
- **人类开发者**: 请读取 `spec/INDEX.md` 获取完整文档索引
- 完整规格按模块拆分在 `spec/` 目录下
- 版本号的唯一来源是 `CLAUDE.md` 的"版本锁定"段
