<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljwx.platform.app.infra.mapper.SysFileMapper">

    <resultMap id="BaseResultMap" type="com.ljwx.platform.app.domain.entity.SysFile">
        <id     column="id"           property="id"/>
        <result column="file_name"    property="fileName"/>
        <result column="file_path"    property="filePath"/>
        <result column="file_size"    property="fileSize"/>
        <result column="file_type"    property="fileType"/>
        <result column="content_type" property="contentType"/>
        <result column="tenant_id"    property="tenantId"/>
        <result column="created_by"   property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_by"   property="updatedBy"/>
        <result column="updated_time" property="updatedTime"/>
        <result column="deleted"      property="deleted"/>
        <result column="version"      property="version"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, file_name, file_path, file_size, file_type, content_type,
        tenant_id, created_by, created_time, updated_by, updated_time, deleted, version
    </sql>

    <insert id="insert" parameterType="com.ljwx.platform.app.domain.entity.SysFile">
        INSERT INTO sys_file (
            id, file_name, file_path, file_size, file_type, content_type,
            tenant_id, created_by, created_time, updated_by, updated_time, deleted, version
        ) VALUES (
            #{id}, #{fileName}, #{filePath}, #{fileSize}, #{fileType}, #{contentType},
            #{tenantId}, #{createdBy}, #{createdTime}, #{updatedBy}, #{updatedTime}, #{deleted}, #{version}
        )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_file
        WHERE id = #{id}
          AND deleted = FALSE
    </select>

    <select id="selectList" parameterType="com.ljwx.platform.app.domain.dto.FileQueryDTO"
            resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_file
        WHERE deleted = FALSE
        <if test="fileName != null and fileName != ''">
            AND file_name LIKE CONCAT('%', #{fileName}, '%')
        </if>
        <if test="fileType != null and fileType != ''">
            AND file_type = #{fileType}
        </if>
        ORDER BY created_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countList" parameterType="com.ljwx.platform.app.domain.dto.FileQueryDTO"
            resultType="long">
        SELECT COUNT(*)
        FROM sys_file
        WHERE deleted = FALSE
        <if test="fileName != null and fileName != ''">
            AND file_name LIKE CONCAT('%', #{fileName}, '%')
        </if>
        <if test="fileType != null and fileType != ''">
            AND file_type = #{fileType}
        </if>
    </select>

    <update id="deleteById">
        UPDATE sys_file SET deleted = TRUE WHERE id = #{id}
    </update>

</mapper>
